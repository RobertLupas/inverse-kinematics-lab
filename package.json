{
  "name": "inverse-kinematics-lab",
  "type": "module",
  "private": true,
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun ./index.html",
    "build": "bun build ./index.html --compile --outdir=dist --target=browser --minify"
  }
}